-- Foreign key constraints for connecting opportunities job finder

-- Note: The main foreign key relationship is already defined inline in the table creation:
-- user_sessions.user_id references users(id) with CASCADE DELETE

-- This file is reserved for additional foreign key constraints that may be added in the future
-- as the database schema evolves with new tables and relationships.